---
"@rhds/elements": minor
---

Elements with lightdom CSS now automatically load their stylesheet into the
document or shadow root when they connect. Manual `<link>` tags are no longer
required but remain supported.
