---
"@rhds/elements": patch
---

`<rh-menu-dropdown>`: Changed the caret icons for basic toggle from `caret-down` and `caret-up` to `caret-down-fill` and `caret-up-fill`. Decreased the caret icon size to match `<rh-select>`'s.