$schema: ../../../docs/schemas/element-data.schema.json
tagName: rh-spinner
name: Spinner
type: element
overallStatus: ready
libraries:
  figma: ready
  rhds: ready
  shared: ready
  docs: ready
relatedItems:
  - form
  - search-bar
  - rh-tag
