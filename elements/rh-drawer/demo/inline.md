The inline variant panel pushes and pulls content in the normal flex flow. When `overlay-threshold` is set on a resizable panel, the drawer automatically switches to overlay positioning once the content area drops below the specified breakpoint, and returns to inline when space is restored. Use the knobs to test position, panel mode, expand, and overlay threshold.
