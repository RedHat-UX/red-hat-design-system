$schema: ../../../docs/schemas/element-data.schema.json
tagName: rh-table
name: Table
type: element
overallStatus: ready
aka:
  - Data table
  - Grid
libraries:
  figma: ready
  rhds: ready
  shared: ready
  docs: ready
relatedItems:
  - rh-code-block
