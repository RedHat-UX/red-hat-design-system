# yaml-language-server: $schema=../../../docs/schemas/element-data.schema.json
type: element
overallStatus: ready
libraries:
  figma: ready
  rhds: ready
  shared: ready
  docs: ready
relatedItems:
  - rh-button
  - rh-badge
  - rh-popover
