# Scheme dropdown

A switch that toggles the state of the color scheme (between light, dark and system default).

## Usage

Use Scheme dropdown when you want to give users the ability to toggle light and dark schemes. Additionally there is a third option to default to the system default setting for the users browser. 

### Basic Scheme dropdown
```html
<rh-scheme-dropdown></rh-scheme-dropdown>
```
